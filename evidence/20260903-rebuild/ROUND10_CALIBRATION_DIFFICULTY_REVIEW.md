# ROUND 10 — A4 캘리브레이션 + 적응형 에코 난이도 + 리뷰 청산

날짜: 2026-09-03

## 1. A4 주파수 캘리브레이션

- Swift: `VocalAudioEngine.referenceA4` (UserDefaults "referenceA4", 435~445Hz 클램프,
  0=미설정=440) — midiNumber(forFrequency:)/frequency(forMidi:) 전체가 따라감
  (노트명·센트·가이드 톤 일관). UI: 트래커 `tuningReferenceBar`(±0.5Hz).
- JS 미러: `a4Ref()` + `setA4` 스테퍼. UserDefaults 매 프레임 읽기는 프로세스 내
  캐시라 비용 무의미 (리뷰 확인).

E2E 실측: 442Hz 입력 — a4=440에서 +7.85¢, a4=442에서 0¢ ✓. 클램프 500→445 ✓.
UI 렌더(기준 피치 행) 양 모드 표시 ✓.

## 2. 적응형 에코 난이도 (Singing Carrots "성공 후에만 상승")

- 레벨 1~3, 이동 집합 L1 ±2~3 / L2 ±2~5 / L3 ±2~7반음. 80%+ 승급,
  2연속 50% 미만 강급, UserDefaults "echoDifficultyLevel" 영속.
- 승급/강급 시 완료 알럿에 안내 문구. 에코 캡션은 레벨 3에서 "최고 난이도"로 분기.

E2E 실측(가짜 시계 드라이버 — 페이지 타이머 스로틀링 회피, 동일 코드 경로):
- L1 스프레드 3(≤3) ✓, 3창 전부 정답 30프레임 → 정확도 100% · 2단계 승급 ✓
- L2 스프레드 [5,3]∈[2,5] ✓, 재성공 → 3단계 ✓
- 2연속 0% → 2단계 강급 (1차 실패 후 유지) ✓

## 3. 리뷰 에이전트 결과 → 전수 수정

P1 4건:
1. Swift A4 버튼 UI 무반응(정적 프로퍼티는 @Observable 추적 대상 아님) →
   `@AppStorage("referenceA4")` 미러로 즉시 갱신.
2. 에코 완료 알럿이 마지막 음만 표시 → `echoTargetLabel`(조인)을 알럿·저장 공용으로 추출.
3. 아픔→좋음 복귀가 탭 재진입시에만 동작 → didSet에 즉시 복구 분기 추가.
4. JS 쉬는 날 전환 직후 시작 시 0초 완주·가짜 세션 → stepEnd를 루틴 길이로 설정
   (setCondition·startRoutine 양경로).

P2/P3 12건 반영: stopTracking 에코 상태 초기화(유령 라인), targetFrequency 계산화
(가이드 톤 캘리브레이션 추종), 무시게이트 해제에 세대 가드, 전화·시리 인터럽션 시
트래커 자동 종료, JS 100점 "D등급" 수정, JS 스킵관통 세션 가드, JS stopTracking
이중호출 가드, 칩 경로 restNotice, 클램프 중복음 방지(범위 필터 추첨), em-dash 2건
제거, "%점"→"%p", 아픔 2일 이상 진료 안내 캡션(Swift·JS), "목표음:"→에코 시 "시작음:",
문체 통일 2건.

회귀 E2E(전 통과): 쉬는 날 시작 후 잔여 120초 유지·세션 미기록 ✓, 스킵 관통
미기록 ✓, 자동·수동 정지 경합 이중 기록 없음 ✓, 복귀 full ✓.

## 4. 검증 (실행 결과)

- `verify_swift_parse.sh` → **통과 32 / 실패 0**
- `verify_static.py` → **ALL CHECKS PASSED**
- `preview/live.html` JS → vm.Script 통과

## 5. 비고

- E2E 중 브라우저 탭 스로틀링(숨김 창)으로 실시간 타이머가 1Hz로 제한되는 환경
  문제 확인 → setTimeout을 가로채는 가짜 시계 드라이버로 동일 코드 경로를
  결정론적으로 검증. 실시간 동작은 ROUND8에서 창 활성 시 이미 실증.
- 리뷰가 지적한 "센트 범례(±12/±35) vs 채점 톨러런스(±25¢)" 불일치는 의도적 유지:
  범례는 지각 임계(표시용), 톨러런스는 채점 기준(기능용)으로 서로 다른 개념.
